<UserControl x:Class="TamponiInventario.Wpf.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Ultimi movimenti" FontSize="20" FontWeight="Bold" Grid.Row="0" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,12" VerticalAlignment="Center">
            <TextBlock Text="Limite righe:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Width="80" Text="{Binding Limit, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Aggiorna" Margin="12,0,0,0" Command="{Binding RefreshCommand}" />
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding History}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}" Width="160" />
                <DataGridTextColumn Header="Azione" Binding="{Binding Action}" Width="80" />
                <DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="120" />
                <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Macchina" Binding="{Binding MachineName}" Width="160" />
                <DataGridTextColumn Header="Note" Binding="{Binding Note}" Width="200" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
