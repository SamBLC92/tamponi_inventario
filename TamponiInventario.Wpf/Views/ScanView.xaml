<UserControl x:Class="TamponiInventario.Wpf.Views.ScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Scan" FontSize="20" FontWeight="Bold" Grid.Row="0" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,12" VerticalAlignment="Center">
            <TextBlock Text="SKU:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Width="220" Text="{Binding Sku, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Invia" Margin="12,0,0,0" Command="{Binding SubmitCommand}" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="Modalità:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <RadioButton Content="Toggle" GroupName="Mode" Margin="0,0,12,0" IsChecked="{Binding IsToggleMode}" />
            <RadioButton Content="Take" GroupName="Mode" Margin="0,0,12,0" IsChecked="{Binding IsTakeMode}" />
            <RadioButton Content="Return" GroupName="Mode" IsChecked="{Binding IsReturnMode}" />
        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                <TextBlock Text="Macchina" FontWeight="SemiBold" />
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0"
                            Visibility="{Binding IsTakeMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ComboBox Width="240"
                              ItemsSource="{Binding Machines}"
                              SelectedItem="{Binding SelectedMachine}"
                              DisplayMemberPath="Name" />
                    <Button Content="Aggiorna" Margin="8,0,0,0" Command="{Binding RefreshMachinesCommand}" />
                </StackPanel>
                <TextBlock Text="Seleziona la macchina per registrare il PRESO." FontStyle="Italic"
                           Visibility="{Binding IsTakeMode, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <Border Margin="0,16,0,0" Padding="12" Background="#F5F5F5" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Text="Risultato" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding ResultMessage}" Margin="0,6,0,0" TextWrapping="Wrap" />
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                            <TextBlock Text="Azione:" FontWeight="SemiBold" />
                            <TextBlock Text=" " />
                            <TextBlock Text="{Binding ResultAction}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                            <TextBlock Text="Macchina:" FontWeight="SemiBold" />
                            <TextBlock Text=" " />
                            <TextBlock Text="{Binding ResultMachineName}" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="Uso tampone" FontWeight="SemiBold" />
                <UniformGrid Columns="2" Margin="0,8,0,0">
                    <TextBlock Text="Giorni correnti" />
                    <TextBlock Text="{Binding CurrentDays}" FontWeight="SemiBold" />
                    <TextBlock Text="Giorni totali" />
                    <TextBlock Text="{Binding TotalDays}" FontWeight="SemiBold" />
                    <TextBlock Text="Soglia avviso" />
                    <TextBlock Text="{Binding WarnDays}" FontWeight="SemiBold" />
                    <TextBlock Text="Soglia allarme" />
                    <TextBlock Text="{Binding AlarmDays}" FontWeight="SemiBold" />
                    <TextBlock Text="Giorni sessione" />
                    <TextBlock Text="{Binding DaysSession}" FontWeight="SemiBold" />
                    <TextBlock Text="Giorni unici aggiunti" />
                    <TextBlock Text="{Binding AddedUniqueDays}" FontWeight="SemiBold" />
                </UniformGrid>

                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                    <Border Background="#FFF8E1" CornerRadius="4" Padding="6,2" Margin="0,0,8,0"
                            Visibility="{Binding Warning, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="Avviso" />
                    </Border>
                    <Border Background="#FFCDD2" CornerRadius="4" Padding="6,2"
                            Visibility="{Binding Alarm, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="Allarme" />
                    </Border>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
