<UserControl x:Class="TamponiInventario.Wpf.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
  <Border Style="{StaticResource CardBorderStyle}">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <StackPanel>
        <TextBlock Text="Storico movimenti" FontSize="20" FontWeight="Bold" />
        <TextBlock Text="Movimenti registrati: PRESO e RESO." Style="{StaticResource MutedTextStyle}" />
      </StackPanel>

      <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0">
        <TextBlock Text="Filtro" VerticalAlignment="Center" Style="{StaticResource MutedTextStyle}" />
        <TextBox Width="260" Margin="8,0,0,0" Text="{Binding FilterQuery, UpdateSourceTrigger=PropertyChanged}" />
        <Button Content="Applica" Margin="8,0,0,0" Padding="12,4" Command="{Binding ApplyFilterCommand}" />
        <Button Content="Aggiorna" Margin="8,0,0,0" Padding="12,4" Command="{Binding RefreshCommand}" />
        <TextBlock Text="Limite" Margin="24,0,0,0" VerticalAlignment="Center" Style="{StaticResource MutedTextStyle}" />
        <TextBox Width="60" Margin="8,0,0,0" Text="{Binding Limit}" />
      </StackPanel>

      <DataGrid Grid.Row="2" Margin="0,16,0,0" ItemsSource="{Binding FilteredMovements}"
                AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" CanUserDeleteRows="False">
        <DataGrid.Columns>
          <DataGridTextColumn Header="Data" Binding="{Binding Timestamp, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="*" />
          <DataGridTemplateColumn Header="Azione" Width="*">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <Border Style="{StaticResource PillWarnStyle}">
                  <TextBlock Text="{Binding Action}" Style="{StaticResource PillText}" />
                </Border>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
          <DataGridTextColumn Header="Tampone" Binding="{Binding SwabName}" Width="2*" />
          <DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="*" />
          <DataGridTextColumn Header="Macchina" Binding="{Binding MachineName}" Width="*" />
          <DataGridTextColumn Header="Note" Binding="{Binding Note}" Width="2*" />
        </DataGrid.Columns>
      </DataGrid>
    </Grid>
  </Border>
</UserControl>
